# -*- mode: snippet -*-
# name: report-fn
# key: reportfn
# expand-env: ((yas/indent-line 'fixed) (yas/also-auto-indent-first-line t))
# --
def parse_metric_${1:name}(row, report)
  case row[:name]
  $0
  else
    write_error(row, 'Unknown action name')
  end
  report
end
